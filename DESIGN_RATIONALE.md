# 编排模式设计理念

> 本文提取自 orchestrator_prompt.md 的设计说明部分，供理解系统设计时参考。
> 操作指令请看 `orchestrator_prompt.md`。

---

## 为什么要拆分？

核心理念：**单Agent做所有事 = 每件事都做得平庸。** 拆分的目的不是并行效率，而是让每个Agent**深度钻研**，产出卓越成果。

**核心设计原则：编排者只定义"做什么"，不预设"怎么做"。** 预设路径/框架/方法 = 在起点就限制了Agent的思维空间。让executor自主设计路径，才能获得真正的多样性。

## 拆分原则：按专业方向拆，不按步骤拆

> 此原则主要适用于模式A（线性策略）。模式B（并行策略）中所有Agent拿同一任务，无需拆分。

| 错误拆法（按步骤） | 正确拆法（按专业深度） |
|-------|-------|
| 步骤1：分析素材 | 专精1：**视频内容解析** — 深度分析素材价值、情绪节奏、可用片段 |
| 步骤2：剪辑 | 专精2：**剪辑技术验证** — 研究最优剪辑插件、参数、效果方案 |
| 步骤3：拼接导出 | 专精3：**拼接与后期** — 研究转场、音效、字幕的最佳实践 |

按步骤拆 = 流水线，每步都浅。按专业拆 = 每个Agent成为该领域的专家。

## 两种模式

### 模式A：线性策略

不同Agent负责**不同方向**，各自产出后**合并**。

```
目标："制作高质量短视频"
├── executor_01 → 内容解析（深度分析素材价值）
├── executor_02 → 剪辑技术（研究最优工具和参数）
└── executor_03 → 后期包装（转场、音效、字幕）
最终：合并三者成果
```

适用于：任务可以清晰拆分为不同专业方向的场景。

### 模式B：并行策略

所有Agent拿到**完全相同的任务和数据**，各自独立设计路径执行。

```
任务："分析课程定价策略"
├── executor_01 → 自主设计分析路径，独立产出
├── executor_02 → 自主设计分析路径，独立产出
└── executor_03 → 自主设计分析路径，独立产出
最终：人类决定是择优、综合、还是混合
```

三个关键约束：
1. **不预设路径** — 不告诉Agent用什么框架/方法，让它自主探索
2. **互不知情** — 每个Agent不知道其他Agent的存在
3. **没有上帝视角** — Agent只知道任务本身，不知道编排层的安排

适用于：路径不确定、需要多样化产出、质量敏感的场景。

### 合并还是竞争？——由人类在审查时决定

这不是编排者预设的，而是人类看到所有结果后的判断：

| 审查后发现 | 人类决策 |
|------------|----------|
| 某个明显更好 | 择优，采用最好的 |
| 各有亮点 | 综合，提炼所有洞察 |
| 互补性强 | 混合，取长补短 |
| 都不行 | 重来，重新拆分 |

### 如何选择？

| 判断条件 | 选模式A（线性） | 选模式B（并行） |
|----------|---------|---------|
| 知道怎么做吗？ | 知道，能拆成清晰的方向 | 不确定，或需要多样化产出 |
| 子任务之间有依赖吗？ | 有，最终要合并 | 无，完全独立 |
| 追求什么？ | 全面覆盖 | 最大化可能性 |
| 结果怎么处理？ | 一定是合并 | 人类到时候再决定 |

**可以混合**：整体用模式A拆分不同方向，其中最难/最不确定的方向用模式B多路并行。

## 为什么要人类反馈？

合并是不可逆的。在合并前让人类确认，避免"辛辛苦苦做完发现不是想要的"。

## 沙箱隔离的意义

类比Git：每个executor就是一个feature branch，在自己的分支上自由工作。主Agent不能自己执行子任务——这样做会污染主Agent的上下文，导致目标漂移。
